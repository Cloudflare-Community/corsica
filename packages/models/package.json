{
	"name": "@corsica/models",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"main": "src/index.ts",
	"scripts": {
		"format": "prettier --write .",
		"lint": "eslint \"**/*.ts*\"",
		"init": "esbuild src/init.ts --bundle --minify --target=esnext --format=esm --outfile=init.js && node init.js && rm init.js"
	},
	"dependencies": {
		"d1-orm": "^0.8.0"
	}
}
